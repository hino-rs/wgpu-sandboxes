# MVP行列

3Dの頂点を2Dな画面に映し出すために、GPUの裏側では3つの行列(4x4の行列)を掛け算している。

1. M (Model): 物体を3D空間のどこに置くか、どれくらい回転させるか。
2. V (View): カメラをどこに置いて、どこに向けるか(LookAt行列)。
3. P (Projection): 人間の目のように、奥に行くほど小さく見える遠近感(透視投影)を付ける行列。

この三つを掛け合わせたMVP行列 (またはVP行列)をCPU側で計算し、GPU側に送ることで、正しい3D描画を実現する。
